// config.js - Central configuration file for the application

// Use relative paths in production for both local and internet access
const API_BASE_URL = "http://nuvolaproject.mooo.com";

// Export configuration values
const config = {
  API_BASE_URL,
  
  // You can add other configuration values here as needed
  // For example:
  // MAX_MESSAGES_PER_PAGE: 50,
  // DEFAULT_LANGUAGE: 'en',
};

export default config;